{
  "name": "@moltstream/orchestrator",
  "version": "0.4.0",
  "description": "Scene graph engine, event queue, deterministic execution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@moltstream/core": "workspace:*"
  },
  "license": "MIT"
}
